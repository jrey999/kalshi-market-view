# Python bytecode cache
__pycache__/
*.pyc

# Credentials
.env

# SQLite working cache and sidecars (data lives in the Spaces bucket, not git)
*.db
*.db-journal
*.db-wal
*.db-shm

# Local Parquet staging tree spaces_export.py writes and spaces_sync.py
# uploads from -- anchored so this doesn't also match data/spaces/, the code
# directory (regenerable)
/data/spaces/staging/

# Per-game report output from kalshi_game_report.py (regenerable)
/data/reports/
